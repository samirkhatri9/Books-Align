const router = require('express').Router()

// Placeholder — full implementation in auth module step
router.get('/', (req, res) => res.json({ message: 'Auth routes coming soon' }))

module.exports = router
