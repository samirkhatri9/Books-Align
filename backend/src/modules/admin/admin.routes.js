const router = require('express').Router()

// Placeholder — full implementation in admin module step
router.get('/', (req, res) => res.json({ message: 'Admin routes coming soon' }))

module.exports = router
