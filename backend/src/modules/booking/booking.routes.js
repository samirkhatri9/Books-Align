const router = require('express').Router()

// Placeholder — full implementation in booking module step
router.get('/', (req, res) => res.json({ message: 'Booking routes coming soon' }))

module.exports = router
